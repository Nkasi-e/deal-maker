/**
 * App config – single entry for routes, env, and shared constants.
 * Import from here: import { ROUTES, DASHBOARD_NAV } from "@/config"
 */
export {
  ROUTES,
  DASHBOARD_NAV,
  isActivePath,
  type RoutePath,
  type NavItem,
} from "./routes";
